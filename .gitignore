# Python
__pycache__/
*.py[cod]
*$py.class
.env
.venv
env/
venv/
build/
dist/
*.egg-info/

# Node
node_modules/
frontend/dist/

# Databases
# We keep verify.db and personalize.db if they contain initial seeds, 
# but usually it's better to ignore them in production.
# For now, I'll ignore them to prevent overwrite on deploy.
*.db

# OS
.DS_Store
