[phases.setup]
nixPkgs = ["python311", "nodejs_22"]

[phases.install]
cmds = ["pip install -r requirements.txt", "npm install --prefix frontend"]

[start]
cmd = "npm run build --prefix frontend && gunicorn --bind 0.0.0.0:$PORT src.app:app"
