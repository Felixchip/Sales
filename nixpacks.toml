[phases.setup]
nixPkgs = ["python311", "nodejs_22"]

[phases.install]
cmds = ["pip install -r requirements.txt", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && ./node_modules/.bin/vite build"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT src.app:app"
