[phases.setup]
nixPkgs = ["python311", "nodejs_22"]

[phases.build]
cmds = ["pip install .", "npm install --prefix frontend", "npm run build --prefix frontend"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT src.app:app"
