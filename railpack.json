{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "node": "22"
  },
  "build": {
    "steps": {
      "build": {
        "commands": [
          "pip install .",
          "cd frontend && npm install && npm run build"
        ]
      }
    }
  },
  "deploy": {
    "startCommand": "python -m gunicorn --bind 0.0.0.0:$PORT src.app:app"
  }
}
